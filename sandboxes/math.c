#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9); // self-explanatory
    double B = pow(2, 4); // first number is the base number, second is the exponent
    int C = round(3.14); // rounds to the nearest whole number
    int D = ceil(3.14); // rounds a number up
    int E = floor(3.99); // opposite of above
    double F = fabs(-100); // finds absolute value
    double G = log(3); // finds logarithm of number
    double H = sin(45); // self-explanatory
    double I = cos(45); // self-explanatory
    double J = tan(45); // self-explanatory

    printf("%lf\n", J);
}
